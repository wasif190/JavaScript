/*
Assume that we gonna use properties of object (i.g- username) in many places so every times we have to do like this:
'user.username', so we can de-structure it for ease of work
*/

const user = {
    username: "John",
    password: 12345,
    isLoggedIn: false
}

/*
const { username } = user
console.log(username)

// If want to give another name
const { username: userName } = user
console.log(userName);
*/








